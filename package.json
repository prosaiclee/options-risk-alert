{
  "name": "options-risk-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Static Options Risk Alert dashboard served on Vercel with Basic Auth.",
  "scripts": {
    "build": "echo 'static dashboard, no build step'"
  }
}
